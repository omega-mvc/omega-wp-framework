<?php

/**
 * Resolver plugin entry file used by the Application package tests.
 *
 * Mirrors the minimal structure of an Omega plugin main file so the
 * ApplicationPlugin structural validation can run in a plain PHPUnit process.
 */

declare(strict_types=1);
