<?php

/**
 * Locale plugin entry file used by the Localization package tests.
 *
 * Mirrors the minimal structure of an Omega plugin main file so the
 * LocalizationServiceProvider can be exercised in a plain PHPUnit process.
 */

declare(strict_types=1);
